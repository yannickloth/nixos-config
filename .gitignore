smb-secrets
.history/
tmp/
result
result-*
.agentmem/

# All secrets live in the gitignored secrets/ folder (see
# secrets-structure/README.md). Keep them out of git — never commit the real
# values. Irreversible user password hashes in users/*.nix stay committed.
secrets/**
